# cargo-fuzz build artefacts
target/
artifacts/
coverage/

# Ignore the dynamic corpus generated by libFuzzer at runtime, but keep
# the small set of hand-picked seed inputs that are committed alongside
# this crate (they bootstrap the fuzzer instead of letting it start
# from zero).
corpus/*/*
!corpus/*/seed_*
